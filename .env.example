# OpenAI-compatible local inference server (vLLM by default).
LLM_BASE_URL=http://127.0.0.1:8000/v1
LLM_MODEL=gemma
# Most local servers ignore this value, but the OpenAI client requires one.
LLM_API_KEY=local
